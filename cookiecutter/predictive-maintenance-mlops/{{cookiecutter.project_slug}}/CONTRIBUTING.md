# Contributing

## Local setup
make setup
pre-commit install

## Standards
- Formatting/linting enforced via pre-commit (ruff + black).
- Keep changes small and well-tested.
- Update docs when behavior changes.

## Useful commands
- Train: `make pipeline`
- API: `make api`
- Tests: `make test`
- Docs: `make docs`
